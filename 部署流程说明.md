# DesignCode-AI 项目部署流程（从 0 到 1）

> **适用场景**：全新服务器从零部署，或代码更新后重新部署。
> **最后更新**：2026-06-29
> **服务器**：腾讯云 Lighthouse 轻量应用服务器（OpenCloudOS）

---

## 一、项目架构概览

```
用户浏览器 (111.229.28.247:80)
        │
        ▼
    ┌─────────┐
    │  Nginx  │  ← 统一入口，端口 80，负责路由分发
    └────┬────┘
         │
    ┌────┴────────────────────┐
    │                         │
    ▼                         ▼
┌─────────────┐         ┌──────────────┐
│ Nuxt (3000) │         │  Go (8888)   │
│  SSR 渲染    │         │  后端 API     │
│  + 解密代理  │         │  + WebSocket │
└──────┬──────┘         └──────────────┘
       │
       │  $fetch 内部调用（解密后转发）
       └───────────────────────────────▶
```

### 双通道 API 架构

| 通道 | 请求路径 | 流转方式 | 用途 |
|------|----------|----------|------|
| **SSR 代理** | `/api/ajaxData` | 浏览器 → Nginx → Nuxt :3000 → 解密 → Go :8888 | 需要加密传输的敏感请求 |
| **直连 API** | `/api/v1/*` | 浏览器 → Nginx → Go :8888 | 普通 API 请求（验证码、登录等） |

> ⚠️ **关键设计**：`/api/ajaxData` 必须走 Nuxt SSR 代理，因为 Nuxt 服务端持有解密密钥，浏览器端不暴露密钥。

---

## 二、项目结构

```
/root/DesignCode-AI/
├── frontend/                  # 前端：Nuxt 4 + Vue 3 (SSR)
│   ├── .env.dev               # 本地开发环境变量
│   ├── .env.prod              # 生产环境变量（⚠️ 部署前需修改 IP）
│   ├── package.json
│   ├── nuxt.config.ts
│   └── .output/               # 构建产物目录
│       └── server/index.mjs   # SSR 入口文件
│
├── frontend_api/              # 后端：Go + Gin + GORM
│   ├── config.dev.yaml        # 本地开发配置
│   ├── config.prod.yaml       # 生产配置（⚠️ 部署前需修改 IP/密码）
│   ├── config/config.go       # 配置加载逻辑（APP_ENV 环境切换）
│   ├── main.go                # 入口
│   ├── server                 # 编译后的二进制文件
│   └── routes/
│       └── auth_route.go      # 路由注册
│
├── nginx.conf                 # Nginx 站点配置
└── 部署流程说明.md             # 本文档
```

---

## 三、环境配置切换机制

### 前端（Nuxt）

| 环境 | 配置文件 | 构建命令 |
|------|---------|---------|
| 本地开发 | `.env.dev` | `npm run dev` |
| 生产构建 | `.env.prod` | `npm run build` |

`.env.prod` 关键变量：
```bash
NUXT_PUBLIC_API_BASE=http://你的服务器IP:8888
NUXT_PUBLIC_WS_BASE=ws://你的服务器IP:8888/ws
NUXT_PUBLIC_DEBUG=false
NUXT_PUBLIC_CRYPTO_KEY=你的32位加密密钥
NUXT_PUBLIC_CRYPTO_IV=你的16位加密向量
```

### 后端（Go）

| 环境 | 配置文件 | 启动命令 |
|------|---------|---------|
| 本地开发 | `config.dev.yaml` | `go run main.go` |
| 生产环境 | `config.prod.yaml` | `APP_ENV=prod ./server` |

`config/config.go` 通过环境变量 `APP_ENV` 判断加载哪个文件：
- 未设置 → 加载 `config.dev.yaml`
- `APP_ENV=prod` → 加载 `config.prod.yaml`

---

## 四、首次部署步骤

### 4.1 服务器环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| 操作系统 | OpenCloudOS / CentOS 7+ | |
| Nginx | 1.28+ | 反向代理，宝塔面板管理 |
| Node.js | 20.x | 通过 nvm 管理 |
| Go | 1.22+ | 编译后端 |
| MySQL | MariaDB 10.11 | 数据库 |
| Git | 2.x | 拉取代码 |

### 4.2 克隆代码

```bash
cd /root
git clone https://你的Token@github.com/For-The-Ideal/DesignCode-AI.git
```

### 4.3 修改配置文件（⚠️ 部署前必须修改）

#### 前端环境变量

```bash
vim /root/DesignCode-AI/frontend/.env.prod
```

将 `111.229.28.247` 替换为你的服务器公网 IP。

#### 后端配置

```bash
vim /root/DesignCode-AI/frontend_api/config.prod.yaml
```

修改以下内容：
- `mysql.dsn`：数据库连接密码
- `server.local`：服务器公网 IP
- `server.jwt_secret`：JWT 密钥
- `ai.models[].api_key`：AI 模型 API Key

### 4.4 构建前端

```bash
cd /root/DesignCode-AI/frontend

# 加载 nvm
export NVM_DIR="/root/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

# 安装依赖并构建
npm install
npm run build

# 验证构建产物
ls -la .output/server/index.mjs
```

### 4.5 构建后端

```bash
cd /root/DesignCode-AI/frontend_api

# 编译（指定输出文件名）
go build -o server main.go

# 验证
ls -la server
```

### 4.6 配置 systemd 服务

#### 后端 API 服务

```bash
cat > /etc/systemd/system/designcode-api.service << 'EOF'
[Unit]
Description=DesignCode-AI Backend API Service
After=network.target mysql.service

[Service]
Type=simple
User=root
WorkingDirectory=/root/DesignCode-AI/frontend_api
ExecStart=/root/DesignCode-AI/frontend_api/server
Restart=always
RestartSec=5
StandardOutput=append:/var/log/designcode-api.log
StandardError=append:/var/log/designcode-api.log
Environment="APP_ENV=prod"

[Install]
WantedBy=multi-user.target
EOF
```

> ⚠️ **注意**：`Environment="APP_ENV=prod"` 必须设置，否则 Go 后端会加载开发环境配置。

#### 前端 Nuxt 服务

```bash
cat > /etc/systemd/system/designcode-frontend.service << 'EOF'
[Unit]
Description=DesignCode-AI Frontend Nuxt Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/root/DesignCode-AI/frontend
EnvironmentFile=/root/DesignCode-AI/frontend/.env.prod
ExecStart=/bin/bash -c 'export NVM_DIR="/root/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" && node .output/server/index.mjs'
Restart=always
RestartSec=5
StandardOutput=append:/var/log/designcode-frontend.log
StandardError=append:/var/log/designcode-frontend.log

[Install]
WantedBy=multi-user.target
EOF
```

> ⚠️ **注意**：
> - `EnvironmentFile` 必须指向 `.env.prod`，否则 Nuxt 无法读取 `NUXT_PUBLIC_*` 环境变量，导致 SSR 代理解密失败。
> - 使用 nvm 管理的 Node.js，需要通过 bash -c 先加载 nvm.sh。

#### 启动服务

```bash
systemctl daemon-reload
systemctl enable designcode-api designcode-frontend
systemctl start designcode-api designcode-frontend

# 检查状态
systemctl status designcode-api designcode-frontend --no-pager -l
```

### 4.7 配置 Nginx 反向代理

> 本服务器使用宝塔面板管理 Nginx，配置文件路径为 `/www/server/nginx/`。
> 站点配置文件通过软链接管理：`/root/DesignCode-AI/nginx.conf` → `/www/server/panel/vhost/nginx/designcode-ai.conf`

```bash
cat > /root/DesignCode-AI/nginx.conf << 'NGINX_EOF'
# DesignCode-AI Nginx Site Configuration
# 反向代理：前端 Nuxt (3000) + 后端 API (8888)

server {
    listen 80;
    server_name 你的服务器IP;

    # 前端静态资源 & 页面（SSR 渲染）
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
        proxy_send_timeout 60s;
    }

    # ⚠️ Nuxt SSR API 代理（ajaxData 路由需要 Nuxt 处理解密和转发）
    # 必须放在 location /api/ 之前，利用精确匹配优先级
    location = /api/ajaxData {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # 后端 API（直连 Go）
    location /api/ {
        proxy_pass http://127.0.0.1:8888/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
        proxy_send_timeout 120s;
    }

    # 日志
    access_log /www/wwwlogs/designcode-ai.access.log;
    error_log /www/wwwlogs/designcode-ai.error.log;
}
NGINX_EOF
```

> ⚠️ **关键规则**：Nginx location 匹配优先级为 `精确匹配(=)` > `前缀匹配(/)`。
> `location = /api/ajaxData` 必须放在 `location /api/` 之前，否则 `/api/ajaxData` 会被 `/api/` 前缀匹配拦截，转发到 Go 后端导致 404。

#### 建立软链接（宝塔面板环境）

```bash
ln -sf /root/DesignCode-AI/nginx.conf /www/server/panel/vhost/nginx/designcode-ai.conf
```

#### 重载 Nginx

```bash
nginx -t && nginx -s reload
```

### 4.8 配置防火墙

腾讯云 Lighthouse 需要在**控制台**或通过 API 开放以下端口：

| 端口 | 协议 | 用途 |
|------|------|------|
| 22 | TCP | SSH 登录 |
| 80 | TCP | HTTP Web 服务 |
| 3000 | TCP | Nuxt SSR（调试用，可选） |
| 8888 | TCP | Go API（调试用，可选） |
| 3306 | TCP | MySQL 远程连接（按需） |

### 4.9 验证部署

```bash
# 1. 检查服务状态
systemctl status designcode-api designcode-frontend --no-pager

# 2. 检查端口监听
ss -tlnp | grep -E '3000|8888'

# 3. 测试 SSR 代理（内部）
curl -s http://127.0.0.1:3000/api/ajaxData | head -c 200

# 4. 测试公网访问
curl -s http://你的服务器IP/api/ajaxData | head -c 200
curl -s http://你的服务器IP/api/v1/auth/captcha | head -c 200

# 5. 浏览器访问
# http://你的服务器IP
```

---

## 五、日常更新部署

### 5.1 标准更新流程

```bash
cd /root/DesignCode-AI

# 1. 拉取最新代码
git pull

# 2. 更新后端
cd frontend_api
go build -o server main.go
systemctl restart designcode-api

# 3. 更新前端
cd ../frontend
export NVM_DIR="/root/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
npm install
npm run build
systemctl restart designcode-frontend

# 4. 检查状态
systemctl status designcode-api designcode-frontend --no-pager -l
```

### 5.2 仅更新前端

```bash
cd /root/DesignCode-AI/frontend
git pull
export NVM_DIR="/root/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
npm install && npm run build
systemctl restart designcode-frontend
```

### 5.3 仅更新后端

```bash
cd /root/DesignCode-AI/frontend_api
git pull
go build -o server main.go
systemctl restart designcode-api
```

### 5.4 仅更新 Nginx 配置

```bash
# 编辑配置
vim /root/DesignCode-AI/nginx.conf

# 测试并重载
nginx -t && nginx -s reload
```

---

## 六、服务管理命令

```bash
# 查看服务状态
systemctl status designcode-api designcode-frontend --no-pager -l

# 重启服务
systemctl restart designcode-api
systemctl restart designcode-frontend

# 停止服务
systemctl stop designcode-api
systemctl stop designcode-frontend

# 查看实时日志
journalctl -u designcode-api -f
journalctl -u designcode-frontend -f

# 查看最近日志
tail -100 /var/log/designcode-api.log
tail -100 /var/log/designcode-frontend.log

# 查看 Nginx 日志
tail -100 /www/wwwlogs/designcode-ai.error.log
tail -100 /www/wwwlogs/designcode-ai.access.log
```

---

## 七、常见问题排查

### 7.1 `/api/ajaxData` 返回 404

**根因**：Nginx 的 `location /api/` 前缀匹配拦截了 `/api/ajaxData`，将其转发到 Go 后端（Go 没有该路由）。

**解决**：
1. 确认 `nginx.conf` 中存在 `location = /api/ajaxData` 精确匹配规则
2. 确认该规则在 `location /api/` **之前**
3. 执行 `nginx -t && nginx -s reload` 重载

### 7.2 前端页面空白 / SSR 渲染失败

**可能原因**：
1. Nuxt 服务未启动 → `systemctl status designcode-frontend`
2. 环境变量未注入 → 检查 `EnvironmentFile` 配置
3. Node.js 未找到 → 确认 nvm 路径正确

**排查**：
```bash
# 检查进程
ps aux | grep node

# 检查日志
tail -50 /var/log/designcode-frontend.log

# 手动启动测试
cd /root/DesignCode-AI/frontend
export NVM_DIR="/root/.nvm" && . "$NVM_DIR/nvm.sh"
node .output/server/index.mjs
```

### 7.3 后端 API 无响应

**可能原因**：
1. Go 服务未启动 → `systemctl status designcode-api`
2. `APP_ENV=prod` 未设置 → 加载了开发配置
3. 数据库连接失败 → 检查 `config.prod.yaml` 中的 DSN

**排查**：
```bash
# 检查进程
ps aux | grep server

# 检查日志
tail -50 /var/log/designcode-api.log

# 测试端口
curl -s http://127.0.0.1:8888/api/v1/auth/captcha
```

### 7.4 Nginx 配置不生效

**可能原因**：
1. 宝塔面板覆盖了配置 → 确认软链接正确
2. 配置语法错误 → `nginx -t` 检查
3. 未重载 → `nginx -s reload`

**排查**：
```bash
# 检查软链接
ls -la /www/server/panel/vhost/nginx/designcode-ai.conf

# 检查 Nginx 主配置是否 include 了站点配置
grep include /www/server/nginx/conf/nginx.conf
```

### 7.5 服务启动失败

```bash
# 查看详细错误
journalctl -u designcode-api -n 50 --no-pager
journalctl -u designcode-frontend -n 50 --no-pager

# 检查端口占用
ss -tlnp | grep -E '3000|8888'

# 手动启动排查
cd /root/DesignCode-AI/frontend_api
APP_ENV=prod ./server
```

---

## 八、端口与架构速查

| 组件 | 端口 | 内部/外部 | 说明 |
|------|------|----------|------|
| Nginx | 80 | 外部 | 统一入口 |
| Nuxt SSR | 3000 | 内部 | 前端渲染 + API 解密代理 |
| Go API | 8888 | 内部 | 后端业务逻辑 |
| MySQL | 3306 | 内部 | 数据库 |

### Nginx 路由规则

| 请求路径 | 转发目标 | 优先级 |
|----------|----------|--------|
| `= /api/ajaxData` | Nuxt :3000 | 最高（精确匹配） |
| `/api/` | Go :8888 | 中（前缀匹配） |
| `/` | Nuxt :3000 | 最低（默认） |

---

## 九、部署检查清单

每次部署完成后，逐项确认：

- [ ] `.env.prod` 中 IP 地址正确
- [ ] `config.prod.yaml` 中数据库密码、IP 正确
- [ ] `designcode-api.service` 中 `APP_ENV=prod` 已设置
- [ ] `designcode-frontend.service` 中 `EnvironmentFile` 指向 `.env.prod`
- [ ] `nginx.conf` 中 `location = /api/ajaxData` 在 `location /api/` 之前
- [ ] `nginx -t` 语法检查通过
- [ ] 两个 systemd 服务均为 `active (running)`
- [ ] `curl http://127.0.0.1:3000/api/ajaxData` 返回 200
- [ ] `curl http://公网IP/api/ajaxData` 返回 200
- [ ] 浏览器访问页面正常渲染

---

## 十、数据库连接

### 通过 SSH 隧道连接

```bash
ssh -L 3307:127.0.0.1:3306 root@你的服务器IP
# 然后本地连接 127.0.0.1:3307
```

### Navicat / DBeaver 配置

- SSH 标签：主机 `你的服务器IP`，端口 22，用户名 root
- 常规标签：主机 `127.0.0.1`，端口 3306，用户名 root，数据库 `design_code_ai`
