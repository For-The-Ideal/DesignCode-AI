# DesignCode-AI 项目部署流程

## 一、项目结构

```
DesignCode-AI/
├── frontend/            # 前端：Nuxt 4 + Vue 3 (SSR)
│   ├── .env.dev         # 本地开发环境变量（在 Git 中）
│   ├── .env.prod        # 生产环境变量（在 Git 中）
│   ├── package.json     # 构建命令
│   └── nuxt.config.js   # Nuxt 配置
│
├── frontend_api/        # 后端：Go + Gin + GORM
│   ├── config.dev.yaml  # 本地开发配置（在 Git 中）
│   ├── config.prod.yaml # 生产配置（在 Git 中）
│   ├── config/config.go # 配置加载逻辑（APP_ENV 环境切换）
│   ├── main.go          # 入口
│   └── internal/        # 业务逻辑
│       ├── app/         # 启动流程
│       ├── handler/     # API 接口
│       ├── worker/      # 队列消费者
│       ├── repository/  # 数据库操作
│       └── skills/      # AI 技能（Vision分析 + Code生成）
└── 部署流程说明.md
```

## 二、环境配置切换机制

### 前端（Nuxt）

| 环境 | 配置文件 | 命令 |
|------|---------|------|
| 本地开发 | `.env.dev` | `npm run dev` (加载 localhost) |
| 生产构建 | `.env.prod` | `npm run build` (加载服务器 IP) |

所有配置文件（`.env.dev`、`.env.prod`、`config.dev.yaml`、`config.prod.yaml`）均提交到 Git 仓库，克隆后直接使用。

### 后端（Go）

| 环境 | 配置文件 | 命令 |
|------|---------|------|
| 本地开发 | `config.dev.yaml` | `go run main.go` |
| 生产环境 | `config.prod.yaml` | `APP_ENV=prod ./server` |

`config/config.go` 通过环境变量 `APP_ENV` 判断加载哪个文件：
- 未设置 → 加载 `config.dev.yaml`
- `APP_ENV=prod` → 加载 `config.prod.yaml`

## 三、部署步骤

### 1. 服务器环境要求

- MySQL（已有，MariaDB 10.11）
- Nginx（已有，1.28）
- Node.js 20（已有）
- Go 1.22.5（已有）
- Git（已有）

### 2. 首次部署（已完成）

```bash
# 克隆仓库
cd /root
git clone https://ghp_TOKEN@github.com/For-The-Ideal/DesignCode-AI.git

# 构建前端
cd /root/DesignCode-AI/frontend
npm install
npm run build

# 构建后端
cd /root/DesignCode-AI/frontend_api
go build -o server main.go

# 配置 systemd 服务
cat > /etc/systemd/system/api.service << 'EOF'
[Unit]
Description=DesignCode AI API
After=network.target mysqld.service

[Service]
Type=simple
WorkingDirectory=/root/DesignCode-AI/frontend_api
ExecStart=/root/DesignCode-AI/frontend_api/server
Restart=always
RestartSec=3
User=root
Environment=APP_ENV=prod

[Install]
WantedBy=multi-user.target
EOF

cat > /etc/systemd/system/frontend.service << 'EOF'
[Unit]
Description=DesignCode AI Frontend
After=network.target

[Service]
Type=simple
WorkingDirectory=/root/DesignCode-AI/frontend
ExecStart=/usr/bin/node /root/DesignCode-AI/frontend/.output/server/index.mjs
Restart=always
RestartSec=3
Environment=NODE_ENV=production
User=root

[Install]
WantedBy=multi-user.target
EOF

# 启动服务
systemctl daemon-reload
systemctl enable api frontend
systemctl start api frontend
```

### 3. 日常更新代码

```bash
# 统一的更新脚本
cd /root/DesignCode-AI
git pull

# 更新后端
cd frontend_api
go build -o server main.go
systemctl restart api

# 更新前端
cd ../frontend
npm install
npm run build
systemctl restart frontend

# 查看状态
systemctl status api frontend --no-pager -l
```

### 4. 服务管理命令

```bash
# 查看实时日志
journalctl -u api -f
journalctl -u frontend -f

# 查看最近日志
journalctl -u api -n 50 --no-pager

# 重启
systemctl restart api
systemctl restart frontend

# 停止
systemctl stop api
systemctl stop frontend
```

## 四、数据库连接

通过 SSH 隧道在本机连接线上数据库：

```bash
ssh -L 3307:127.0.0.1:3306 root@111.229.28.247
# 然后本地连接 127.0.0.1:3307
```

或使用 Navicat 配置 SSH 通道：
- SSH 标签：主机 111.229.28.247，端口 22，用户名 root
- 常规标签：主机 127.0.0.1，端口 3306，用户名 root，数据库 design_code_ai

## 五、端口说明

| 服务 | 端口 | 说明 |
|------|------|------|
| Nginx | 80 | 前端反向代理（http://111.229.28.247） |
| Nuxt SSR | 3000 | 前端服务（Nginx 代理到 80） |
| Go API | 8888 | 后端 API 服务 |
| MySQL | 3306 | 数据库（仅内网） |
